@extends('layouts.app')
@section('title', 'Provider Verification')
@section('content')
<div class="container py-4"><div class="d-flex justify-content-between align-items-center mb-3"><h1 class="h3">Provider Verification</h1><form class="d-flex gap-2"><select class="form-select" name="status"><option value="">All verification states</option>@foreach(['pending','verified','rejected'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst($option) }}</option>@endforeach</select><button class="btn btn-outline-primary">Filter</button></form></div>@if($providers->isEmpty())<x-ui.empty-state title="No provider submissions" message="There are no provider records for this filter." />@else<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Business</th><th>Type</th><th>Owner</th><th>Verification</th><th>Activation</th><th></th></tr></thead><tbody>@foreach($providers as $provider)<tr><td>{{ $provider->business_name }}</td><td>{{ str_replace('_', ' ', ucfirst($provider->provider_type)) }}</td><td>{{ $provider->user?->email }}</td><td><x-ui.status-badge :status="$provider->verification_status" /></td><td><x-ui.status-badge :status="$provider->status" /></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('bureau.providers.show', $provider) }}">Review</a></td></tr>@endforeach</tbody></table></div>@endif</div>
@endsection
