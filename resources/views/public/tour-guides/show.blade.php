@extends('layouts.app')

@section('title', 'Tour Guide Profile')

@section('content')
<div class="container py-4 py-lg-5"><a class="small text-decoration-none" href="{{ route('tour-guides.index') }}">&larr; All tour guides</a><div class="row g-4 mt-1"><div class="col-lg-8"><article class="card border-0 shadow-sm"><div class="card-body p-4 p-md-5"><div class="d-flex flex-wrap justify-content-between gap-2 mb-3"><span class="badge text-bg-success">Verified guide</span><span class="badge text-bg-{{ $guide->availability_status === 'available' ? 'success' : 'secondary' }}">{{ ucfirst($guide->availability_status) }}</span></div><h1 class="h2">Tour Guide Profile</h1><p class="text-muted mb-4">License {{ $guide->license_number }}</p><h2 class="h5">Areas of expertise</h2><p class="mb-4">{{ $guide->expertise }}</p><h2 class="h5">Rating</h2><p class="mb-0">{{ $guide->average_rating === null ? 'No ratings yet' : number_format((float) $guide->average_rating, 1).' / 5' }}</p></div></article></div><div class="col-lg-4"><div class="card border-0 shadow-sm"><div class="card-body p-4"><h2 class="h5">Request a tour</h2><p class="small text-muted">Choose dates and party size to send a pending request to this verified guide.</p>@auth @if (auth()->user()->role === 'tourist' && $guide->availability_status === 'available')<a class="btn btn-primary w-100" href="{{ route('tour-guides.book', $guide) }}">Book this guide</a>@elseif (auth()->user()->role === 'tourist')<div class="alert alert-secondary small mb-0">This guide is currently unavailable.</div>@else<div class="alert alert-info small mb-0">Only tourists can submit guide requests.</div>@endif @else<a class="btn btn-primary w-100" href="{{ route('login') }}">Log in to request</a>@endauth</div></div></div></div></div>
@endsection
