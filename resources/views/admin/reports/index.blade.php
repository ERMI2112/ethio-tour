@extends('layouts.app')
@section('title', 'Reports and Analytics')
@section('content')
<div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-muted small text-uppercase mb-1">Administrator</p><h1 class="h3 mb-0">Reports and analytics</h1></div></div><form class="card border-0 shadow-sm mb-4"><div class="card-body row g-3 align-items-end"><div class="col-md-3"><label class="form-label" for="from">From</label><input class="form-control" type="date" id="from" name="from" value="{{ $from }}"></div><div class="col-md-3"><label class="form-label" for="to">To</label><input class="form-control" type="date" id="to" name="to" value="{{ $to }}"></div><div class="col-md-3"><label class="form-label" for="status">Booking status</label><select class="form-select" id="status" name="status"><option value="">All statuses</option>@foreach(['pending','accepted','rejected','payment_pending','confirmed','cancelled','completed'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst(str_replace('_',' ', $option)) }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-primary w-100">Apply filters</button></div></div></form><div class="row g-3 mb-4">@foreach([['Total users',$totalUsers],['Active users',$activeUsers],['Bookings',$bookingTotal],['Reviews',$reviewCount],['Average rating',$reviewAverage === null ? '—' : number_format((float) $reviewAverage, 1)]] as [$label,$value])<div class="col-6 col-xl"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="small text-muted">{{ $label }}</div><div class="display-6 fw-semibold">{{ $value }}</div></div></div></div>@endforeach</div><div class="row g-4"><div class="col-lg-6"><div class="card border-0 shadow-sm h-100"><div class="card-header bg-white"><h2 class="h5 mb-0">Users by role</h2></div><div class="card-body"><ul class="list-group list-group-flush">@foreach($roleBreakdown as $role => $total)<li class="list-group-item d-flex justify-content-between px-0"><span>{{ str_replace('_',' ', ucfirst($role)) }}</span><strong>{{ $total }}</strong></li>@endforeach</ul></div></div></div><div class="col-lg-6"><div class="card border-0 shadow-sm h-100"><div class="card-header bg-white"><h2 class="h5 mb-0">Booking status</h2></div><div class="card-body"><ul class="list-group list-group-flush">@foreach($statusBreakdown as $state => $total)<li class="list-group-item d-flex justify-content-between px-0"><span>{{ ucfirst(str_replace('_',' ', $state)) }}</span><strong>{{ $total }}</strong></li>@endforeach</ul></div></div></div></div><div class="card border-0 shadow-sm mt-4"><div class="card-header bg-white"><h2 class="h5 mb-0">Bookings by domain</h2></div><div class="card-body row g-3">@foreach($domainBookings as $domain => $total)<div class="col-6 col-md-4 col-xl"><div class="border rounded p-3"><div class="small text-muted">{{ ucfirst($domain) }}</div><strong class="fs-4">{{ $total }}</strong></div></div>@endforeach</div></div></div>
@endsection
