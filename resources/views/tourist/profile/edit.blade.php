@extends('layouts.app')
@section('title', 'Edit Profile')
@section('content')
<div class="container py-4 py-lg-5"><div class="mb-4"><a class="small" href="{{ route('tourist.profile') }}">&larr; Back to profile</a><h1 class="h2 mt-2 mb-1">Edit Profile</h1><p class="text-muted mb-0">Update only the personal details supported by your tourist profile.</p></div><form method="POST" action="{{ route('tourist.profile.update') }}" class="card border-0 shadow-sm"><div class="card-body p-4 col-lg-8"><div class="mb-3"><label class="form-label" for="full_name">Full name</label><input class="form-control @error('full_name') is-invalid @enderror" id="full_name" name="full_name" value="{{ old('full_name', $tourist->full_name) }}" required>@error('full_name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="mb-3"><label class="form-label" for="nationality">Nationality</label><input class="form-control @error('nationality') is-invalid @enderror" id="nationality" name="nationality" value="{{ old('nationality', $tourist->nationality) }}" required>@error('nationality')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="d-flex gap-2"><button class="btn btn-primary" type="submit">Save profile</button><a class="btn btn-outline-secondary" href="{{ route('tourist.profile') }}">Cancel</a></div></div></form></div>
@endsection
