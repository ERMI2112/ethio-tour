@extends('layouts.app')
@section('title', 'Review Guide')
@section('content')
<div class="container py-4"><a href="{{ route('bureau.guides.index') }}">&larr; Guide verification</a><div class="card shadow-sm mt-3"><div class="card-body"><h1 class="h4">{{ $guide->user?->email }}</h1><dl class="row"><dt class="col-sm-3">License</dt><dd class="col-sm-9">{{ $guide->license_number }}</dd><dt class="col-sm-3">Expertise</dt><dd class="col-sm-9">{{ $guide->expertise }}</dd><dt class="col-sm-3">Availability</dt><dd class="col-sm-9">{{ ucfirst($guide->availability_status) }}</dd><dt class="col-sm-3">Status</dt><dd class="col-sm-9"><x-ui.status-badge :status="$guide->verification_status" /></dd><dt class="col-sm-3">Notes</dt><dd class="col-sm-9">{{ $guide->verification_notes ?: '—' }}</dd></dl>@if($guide->verification_status === 'pending')<form method="POST" action="{{ route('bureau.guides.decide', $guide) }}" class="border-top pt-3">@csrf @method('PATCH')<div class="mb-3"><label class="form-label" for="verification_notes">Decision notes</label><textarea class="form-control @error('verification_notes') is-invalid @enderror" id="verification_notes" name="verification_notes" rows="3">{{ old('verification_notes') }}</textarea>@error('verification_notes')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><button class="btn btn-success" name="decision" value="approve">Approve guide</button><button class="btn btn-outline-danger ms-2" name="decision" value="reject">Reject guide</button></form>@endif</div></div></div>
@endsection
