@extends('layouts.app')
@section('title', 'Restaurant Services')
@section('content')
<div class="container py-4 py-lg-5"><nav aria-label="breadcrumb"><ol class="breadcrumb small"><li class="breadcrumb-item"><a href="{{ route('restaurant.dashboard') }}">Restaurant Dashboard</a></li><li class="breadcrumb-item active">Services</li></ol></nav><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h2 mb-1">Menu and service offerings</h1><p class="text-muted mb-0">Restaurant offerings use the shared TourismService architecture.</p></div><a class="btn btn-primary" href="{{ route('restaurant.services.create') }}">Add service</a></div>@if($services->isEmpty())<x-ui.empty-state title="No restaurant services yet" message="Add your first menu or dining offering." />@else<div class="row g-3">@foreach($services as $service)<div class="col-md-6 col-xl-4"><div class="card border-0 shadow-sm h-100"><div class="card-body p-4"><div class="d-flex justify-content-between gap-2"><h2 class="h5">{{ $service->service_name }}</h2><strong class="text-primary text-nowrap">{{ number_format($service->price, 2) }} ETB</strong></div><div class="small text-muted">{{ $service->category->category_name }} · {{ $service->destination->name }}</div><p class="small text-muted mt-3">{{ Str::limit($service->description, 140) }}</p><div class="d-flex gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('restaurant.services.edit', $service) }}">Edit</a><form method="POST" action="{{ route('restaurant.services.destroy', $service) }}" onsubmit="return confirm('Remove this restaurant service?');">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form></div></div></div></div>@endforeach</div>@endif</div>
@endsection
