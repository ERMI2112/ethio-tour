@extends('layouts.app')

@section('title', 'Guide Booking Requests')

@section('content')
<div class="container py-4 py-lg-5"><div class="row g-4"><div class="col-lg-3">@include('tour-guide.partials.sidebar')</div><div class="col-lg-9"><nav aria-label="breadcrumb"><ol class="breadcrumb small"><li class="breadcrumb-item"><a href="{{ route('tour-guide.dashboard') }}">Dashboard</a></li><li class="breadcrumb-item active" aria-current="page">Booking Requests</li></ol></nav><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><p class="text-muted small text-uppercase mb-1">Tour Guide Portal</p><h1 class="h2 mb-0">Booking Requests</h1></div><a class="btn btn-outline-primary" href="{{ route('tour-guide.availability') }}">View availability</a></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body row g-2 align-items-end"><div class="col-sm-6"><label class="form-label" for="status">Filter by status</label><select class="form-select" id="status" name="status"><option value="">All statuses</option>@foreach (['pending', 'accepted', 'payment_pending', 'confirmed', 'rejected', 'cancelled', 'completed'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst(str_replace('_', ' ', $option)) }}</option>@endforeach</select></div><div class="col-sm-auto"><button class="btn btn-primary" type="submit">Apply filter</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Tourist</th><th>Tour dates</th><th>Party size</th><th>Status</th><th>Requested</th><th><span class="visually-hidden">Action</span></th></tr></thead><tbody>@forelse ($bookings as $booking)<tr><td>{{ $booking->tourist?->full_name }}</td><td>{{ $booking->tourGuideReservation->start_date->format('M j, Y') }}<br><span class="text-muted small">to {{ $booking->tourGuideReservation->end_date->format('M j, Y') }}</span></td><td>{{ $booking->tourGuideReservation->number_of_tourists }}</td><td><x-ui.status-badge :status="$booking->status" /></td><td>{{ $booking->booking_date?->format('M j, Y') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('tour-guide.requests.show', $booking) }}">View</a></td></tr>@empty<tr><td class="text-center text-muted py-4" colspan="6">No guide booking requests match this filter.</td></tr>@endforelse</tbody></table></div>@if ($bookings->hasPages())<div class="card-footer bg-white">{{ $bookings->links() }}</div>@endif</div></div></div></div>
@endsection
