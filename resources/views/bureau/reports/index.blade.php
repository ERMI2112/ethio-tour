@extends('layouts.app')
@section('title', 'Bureau Reports')
@section('content')<div class="container py-4"><h1 class="h3 mb-4">Verification reports</h1><div class="row g-4"><div class="col-lg-6"><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Guide verification</h2></div><div class="card-body">@foreach(['pending','verified','rejected'] as $state)<div class="d-flex justify-content-between border-bottom py-2"><span>{{ ucfirst($state) }}</span><strong>{{ $guideStates[$state] ?? 0 }}</strong></div>@endforeach</div></div></div><div class="col-lg-6"><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Provider verification</h2></div><div class="card-body">@foreach(['pending','verified','rejected'] as $state)<div class="d-flex justify-content-between border-bottom py-2"><span>{{ ucfirst($state) }}</span><strong>{{ $providerStates[$state] ?? 0 }}</strong></div>@endforeach</div></div></div></div><div class="card border-0 shadow-sm mt-4"><div class="card-header bg-white"><h2 class="h5 mb-0">Recent Bureau decisions</h2></div><div class="card-body">@forelse($recentDecisions as $decision)<div class="d-flex justify-content-between border-bottom py-2"><span>{{ str_replace('_',' ', ucfirst($decision->action)) }}</span><small class="text-muted">{{ $decision->created_at?->diffForHumans() }}</small></div>@empty<p class="text-muted mb-0">No Bureau decisions recorded.</p>@endforelse</div></div></div>@endsection
