@extends('layouts.app')
@section('title', 'Restaurant Tables')
@section('content')<div class="container py-4 py-lg-5"><nav aria-label="breadcrumb"><ol class="breadcrumb small"><li class="breadcrumb-item"><a href="{{ route('restaurant.dashboard') }}">Restaurant Dashboard</a></li><li class="breadcrumb-item active">Tables</li></ol></nav><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h2 mb-1">Table inventory</h1><p class="text-muted mb-0">Availability is calculated by date and time; there is no permanent available flag.</p></div><a class="btn btn-primary" href="{{ route('restaurant.tables.create') }}">Add table</a></div>@if($tables->isEmpty())<x-ui.empty-state title="No tables yet" message="Add physical tables with their seating capacities." />@else<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Table</th><th>Capacity</th><th>Status</th><th>Reservation history</th><th class="text-end">Actions</th></tr></thead><tbody>@foreach($tables as $table)<tr><td class="fw-semibold">{{ $table->table_number }}</td><td>{{ $table->capacity }} guest(s)</td><td><span class="badge text-bg-{{ $table->status === 'active' ? 'success' : 'secondary' }}">{{ ucfirst($table->status) }}</span></td><td>{{ $table->restaurant_reservations_count }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('restaurant.tables.edit', $table) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('restaurant.tables.destroy', $table) }}" onsubmit="return confirm('Remove this table?');">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form></td></tr>@endforeach</tbody></table></div></div>@endif</div>@endsection
