@extends('layouts.app')
@section('title', 'Transportation Dashboard')
@section('content')
<div class="container py-4 py-lg-5"><div class="ws-page-header mb-4"><div><span class="ws-eyebrow"><span class="ws-eye-dot" aria-hidden="true"></span>Fleet operations</span><h1 class="ws-title">{{ $provider->business_name }}</h1><p class="ws-lead">Manage services, vehicles, and rental requests.</p></div><div class="ws-actions"><a class="btn btn-primary" href="{{ route('transportation.profile') }}">View profile</a></div></div><section aria-labelledby="attention-heading" class="mb-4"><h2 id="attention-heading" class="ws-section-title mb-3">Needs attention</h2>@if($stats['pendingReservations'] > 0)<div class="alert alert-warning d-flex flex-wrap justify-content-between align-items-center gap-3"><div><strong>{{ $stats['pendingReservations'] }} rental request(s) waiting</strong><div class="small">Review dates and vehicle availability before deciding.</div></div><a class="btn btn-warning" href="{{ route('transportation.reservations.index', ['status'=>'pending']) }}">Review reservations</a></div>@else<div class="alert alert-success"><strong>No rental requests are waiting.</strong> New requests will appear in Reservations.</div>@endif</section><div class="row g-3 mb-4">@foreach ([['Services',$stats['serviceCount'],'transportation.services.index'],['Vehicles',$stats['vehicleCount'],'transportation.vehicles.index'],['Active vehicles',$stats['activeVehicles'],'transportation.vehicles.index'],['Pending requests',$stats['pendingReservations'],'transportation.reservations.index']] as [$label,$value,$route])<div class="col-sm-6 col-xl-3"><a class="card border-0 shadow-sm h-100 text-decoration-none" href="{{ route($route) }}"><div class="card-body"><div class="ws-stat-label">{{ $label }}</div><div class="ws-stat-value text-primary">{{ $value }}</div></div></a></div>@endforeach</div><div class="card border-0 shadow-sm"><div class="card-body"><h2 class="h5">Fleet overview</h2><p class="text-muted mb-0">Active and inactive vehicles are date-specific inventory. Availability is checked when a rental request is accepted.</p></div></div></div>
@endsection
