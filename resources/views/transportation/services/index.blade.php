@extends('layouts.app')
@section('title', 'Transportation Services')
@section('content')<div class="container py-4"><div class="d-flex justify-content-between mb-4"><div><h1 class="h3">Transportation services</h1><p class="text-muted">Publish rental and transfer offerings.</p></div><a class="btn btn-primary" href="{{ route('transportation.services.create') }}">Add service</a></div>@if($services->isEmpty())<x-ui.empty-state title="No services yet" message="Create a service before adding vehicles." />@else<div class="table-responsive"><table class="table bg-white shadow-sm align-middle"><thead><tr><th>Service</th><th>Category</th><th>Destination</th><th>Price</th><th>Actions</th></tr></thead><tbody>@foreach($services as $service)<tr><td>{{ $service->service_name }}</td><td>{{ $service->category->category_name }}</td><td>{{ $service->destination->name }}</td><td>{{ number_format($service->price,2) }} ETB</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('transportation.services.edit',$service) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('transportation.services.destroy',$service) }}" data-confirm="Remove this service?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form></td></tr>@endforeach</tbody></table></div>@endif</div>@endsection
