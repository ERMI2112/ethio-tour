@extends('layouts.app')

@section('title', 'Transportation Dashboard · '.$provider->business_name)

@section('content')
<div class="container-fluid py-4 py-lg-5 px-3 px-lg-5">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4 pb-3 border-bottom"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Transportation portal</p><h1 class="h3 mb-1">{{ $provider->business_name }}</h1><p class="text-secondary mb-0">{{ $provider->user?->email }}</p></div><div class="d-flex gap-2"><a class="btn btn-dark btn-sm" href="{{ route('transportation.vehicles.create') }}">Add vehicle</a><a class="btn btn-outline-secondary btn-sm" href="{{ route('transportation.profile') }}">Profile</a></div></div>
    <h2 class="h5 mb-3">Needs attention</h2>
    @if($stats['pendingReservations'] > 0)<div class="alert alert-warning">{{ $stats['pendingReservations'] }} rental request(s) are waiting for review. <a href="{{ route('transportation.reservations.index', ['status' => 'pending']) }}">Review requests</a></div>@else<div class="alert alert-success">No rental requests are waiting.</div>@endif
    <div class="row g-3 mb-4">
        <div class="col-6 col-xl-3"><div class="card border-0 shadow-sm rounded-4 h-100 p-3"><span class="small text-muted text-uppercase fw-bold">Active vehicles</span><strong class="h3 mt-2">{{ $stats['activeVehicles'] }} / {{ $stats['vehicleCount'] }}</strong><span class="small text-muted">Current inventory records</span></div></div>
        <div class="col-6 col-xl-3"><div class="card border-0 shadow-sm rounded-4 h-100 p-3"><span class="small text-muted text-uppercase fw-bold">Trips today</span><strong class="h3 mt-2">{{ $stats['scheduledTripsToday'] }}</strong><span class="small text-muted">Accepted, payment-pending, or confirmed</span></div></div>
        <div class="col-6 col-xl-3"><div class="card border-0 shadow-sm rounded-4 h-100 p-3"><span class="small text-muted text-uppercase fw-bold">This month revenue</span><strong class="h3 mt-2">{{ number_format($stats['escrowRevenue'], 2) }} ETB</strong><span class="small text-muted">Confirmed and completed bookings</span></div></div>
        <div class="col-6 col-xl-3"><div class="card border-0 shadow-sm rounded-4 h-100 p-3"><span class="small text-muted text-uppercase fw-bold">Reviews</span>@if($stats['reviewCount'] > 0)<strong class="h3 mt-2">{{ number_format($stats['reviewAverage'], 1) }} / 5</strong><span class="small text-muted">{{ $stats['reviewCount'] }} recorded</span>@else<strong class="h5 mt-2">No reviews yet</strong><span class="small text-muted">No review records</span>@endif</div></div>
    </div>
    <div class="row g-4">
        <div class="col-lg-7"><div class="card border-0 shadow-sm rounded-4"><div class="card-header bg-white d-flex justify-content-between"><h2 class="h5 mb-0">Fleet overview</h2><a href="{{ route('transportation.vehicles.index') }}" class="small">Manage</a></div><div class="list-group list-group-flush">@forelse($vehicles as $vehicle)<div class="list-group-item d-flex justify-content-between gap-3"><div><strong>{{ $vehicle->vehicle_identifier }}</strong><small class="d-block text-muted">{{ trim($vehicle->make.' '.$vehicle->model) ?: 'Vehicle details not provided' }} · capacity {{ $vehicle->capacity ?: 'not set' }}</small></div><x-ui.status-badge :status="$vehicle->status" /></div>@empty<div class="list-group-item text-muted">No vehicles have been added yet.</div>@endforelse</div></div></div>
        <div class="col-lg-5"><div class="card border-0 shadow-sm rounded-4"><div class="card-header bg-white"><h2 class="h5 mb-0">Recent rental requests</h2></div><div class="list-group list-group-flush">@forelse($recentRequests as $booking)<div class="list-group-item"><strong>{{ $booking->tourismService?->service_name ?: 'Transportation service' }}</strong><small class="d-block text-muted">{{ $booking->tourist?->full_name ?: $booking->tourist?->user?->email ?: 'Tourist' }} · booking #{{ $booking->booking_id }}</small><span class="badge text-bg-light border mt-1">{{ ucfirst($booking->status) }}</span></div>@empty<div class="list-group-item text-muted">No rental requests recorded yet.</div>@endforelse</div><div class="card-footer bg-white"><a href="{{ route('transportation.reservations.index') }}" class="small">View all requests</a></div></div></div>
    </div>
</div>
@endsection
