@extends('layouts.app')
@section('title', 'Transportation Reservations')
@section('content')
<div class="container-fluid py-4 py-lg-5"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Fleet operations</p><h1 class="h2 mb-1">Rental reservations</h1><p class="text-muted mb-0">Only reservations for your transportation services are shown.</p></div><div class="card border-0 shadow-sm mb-4"><div class="card-body py-2"><ul class="nav nav-pills flex-wrap"><li class="nav-item"><a class="nav-link {{ empty($status) ? 'active' : '' }}" href="{{ route('transportation.reservations.index') }}">All</a></li>@foreach(['pending'=>'Pending','accepted'=>'Accepted','payment_pending'=>'Awaiting Payment','confirmed'=>'Confirmed','cancelled'=>'Cancelled','completed'=>'Completed'] as $value => $label)<li class="nav-item"><a class="nav-link {{ $status === $value ? 'active' : '' }}" href="{{ route('transportation.reservations.index', ['status'=>$value]) }}">{{ $label }}</a></li>@endforeach</ul></div></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Customer</th><th>Route</th><th>Rental window</th><th>Vehicle</th><th>Status</th><th><span class="visually-hidden">Action</span></th></tr></thead><tbody>@forelse($bookings as $booking)@php($res=$booking->transportationReservation)<tr><td class="fw-semibold">{{ $booking->tourist->full_name ?? $booking->tourist->user->email ?? 'Customer' }}</td><td>{{ $res->pickup_location }} → {{ $res->dropoff_location }}</td><td class="text-nowrap">{{ $res->pickup_at->format('M d, Y H:i') }} – {{ $res->dropoff_at->format('M d, Y H:i') }}</td><td>{{ $res->vehicle?->vehicle_identifier ?? 'Unassigned' }}</td><td><x-ui.status-badge :status="$booking->status" /></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('transportation.reservations.show',$booking) }}">View</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No rental reservations match this filter. New requests will appear here.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $bookings->links() }}</div></div></div>
@endsection
