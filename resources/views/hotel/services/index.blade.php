@extends('layouts.app')

@section('title', 'Room-Type Services')

@section('content')
<div class="container py-5"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><a class="small text-decoration-none" href="{{ route('hotel.profile') }}">&larr; Hotel management</a><h1 class="h2 mt-2 mb-1">Room-type services</h1><p class="text-muted mb-0">Each service represents a bookable hotel room type and uses the existing nightly price.</p></div><a class="btn btn-primary" href="{{ route('hotel.services.create') }}">Add room type</a></div>@if($services->isEmpty())<x-ui.empty-state title="No room-type services yet" message="Add your first Standard, Deluxe, or Suite room type." />@else<div class="table-responsive"><table class="table align-middle bg-white shadow-sm"><thead><tr><th>Room type</th><th>Category</th><th>Destination</th><th>Capacity</th><th>Rooms</th><th>Nightly price</th><th class="text-end">Actions</th></tr></thead><tbody>@foreach($services as $service)<tr><td><strong>{{ $service->service_name }}</strong><div class="small text-muted">{{ \Illuminate\Support\Str::limit($service->description, 70) }}</div></td><td>{{ $service->category->category_name }}</td><td>{{ $service->destination->name }}</td><td>{{ $service->hotelRoomType?->capacity ?? '—' }}</td><td>{{ $service->hotelRoomType?->hotelRooms->count() ?? 0 }}</td><td>{{ number_format($service->price, 2) }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('hotel.services.edit', $service) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('hotel.services.destroy', $service) }}" onsubmit="return confirm('Remove this room-type service?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" type="submit">Remove</button></form></td></tr>@endforeach</tbody></table></div>@endif</div>
@endsection
