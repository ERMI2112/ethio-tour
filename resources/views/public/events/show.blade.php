@extends('layouts.app')
@section('title',$event->event_name)
@section('content')<div class="container py-4"><a class="link-secondary" href="{{ route('events.index') }}">&larr; Back to events</a><div class="row g-4 mt-1"><div class="col-lg-7"><div class="card border-0 shadow-sm"><div class="card-body p-4"><p class="text-uppercase small text-primary fw-semibold mb-1">Cultural event</p><h1 class="h2">{{ $event->event_name }}</h1><p class="text-muted">{{ $event->destination->name }} · {{ $event->venue }}</p><p>{{ $event->description }}</p><dl class="row"><dt class="col-sm-4">Date</dt><dd class="col-sm-8">{{ $event->event_date->format('F d, Y') }}</dd><dt class="col-sm-4">Schedule</dt><dd class="col-sm-8">{{ $event->start_time }} – {{ $event->end_time }}</dd></dl></div></div></div><div class="col-lg-5"><div class="card border-0 shadow-sm"><div class="card-body p-4"><h2 class="h5">Tickets</h2>@forelse($event->ticketTypes as $ticket)<div class="border-bottom py-3"><div class="d-flex justify-content-between"><strong>{{ $ticket->name }}</strong><span>{{ number_format($ticket->price,2) }} ETB</span></div><div class="small text-muted mb-2">{{ app(\App\Services\EventInventoryService::class)->availableQuantity($ticket) }} remaining</div>@auth @if(auth()->user()->role==='tourist')<form method="POST" action="{{ route('tourist.event-reservations.store',$event) }}">@csrf<input type="hidden" name="ticket_type_id" value="{{ $ticket->ticket_type_id }}"><div class="input-group"><input class="form-control" type="number" name="quantity" min="1" value="1"><button class="btn btn-primary">Book</button></div></form>@endif @else<p class="small text-muted mb-0">Log in as a tourist to book.</p>@endauth</div>@empty<p class="text-muted mb-0">Tickets are not available yet.</p>@endforelse</div></div></div></div><section class="card border-0 shadow-sm mt-4"><div class="card-body p-4"><div class="d-flex justify-content-between align-items-center mb-3"><h2 class="h5 mb-0">Attendee reviews</h2><x-reviews.rating-summary :average="$reviewAverage" :count="$reviewCount" /></div><x-reviews.review-list :reviews="$reviews" /></div></section></div>@endsection
