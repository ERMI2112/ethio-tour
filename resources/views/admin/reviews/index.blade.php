@extends('layouts.app')
@section('title', 'Review Moderation')
@section('content')
<div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-muted small text-uppercase mb-1">Administrator</p><h1 class="h3 mb-0">Review moderation</h1></div></div>@if($reviews->isEmpty())<x-ui.empty-state title="No reviews" message="Submitted reviews will appear here for moderation." />@else<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Reviewer</th><th>Target</th><th>Rating</th><th>Comment</th><th>Date</th><th></th></tr></thead><tbody>@foreach($reviews as $review)<tr><td>{{ $review->tourist?->full_name ?? 'Unknown' }}</td><td>{{ $review->booking?->tourGuide ? 'Tour Guide' : ($review->booking?->tourismService?->service_name ?? 'Booking') }}</td><td><x-reviews.star-rating :rating="$review->rating" /></td><td>{{ Str::limit($review->comment, 100) }}</td><td>{{ $review->review_date?->format('Y-m-d') }}</td><td><form method="POST" action="{{ route('admin.reviews.destroy', $review) }}" onsubmit="return confirm('Remove this review?');">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $reviews->links() }}</div>@endif</div>
@endsection
