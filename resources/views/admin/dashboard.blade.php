@extends('layouts.app')
@section('title', 'Administrator Dashboard')
@section('content')
<div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-uppercase text-muted small mb-1">Platform operations</p><h1 class="h3 mb-0">Administrator Dashboard</h1></div><span class="badge text-bg-success">Operational</span></div><div class="row g-3 mb-4">@foreach([['Total users',$totalUsers],['Active users',$activeUsers],['Pending actions',$pendingActions],['Pending activation',$pendingProviders],['Approved providers',$approvedProviders],['Suspended providers',$suspendedProviders],['Verified guides',$verifiedGuides],['Active bookings',$bookings]] as [$label,$value])<div class="col-6 col-md-4 col-xl-3"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-muted small">{{ $label }}</div><div class="display-6 fw-semibold">{{ $value }}</div></div></div></div>@endforeach</div><div class="row g-4"><div class="col-lg-7"><div class="card border-0 shadow-sm"><div class="card-body"><div class="d-flex justify-content-between"><h2 class="h5">Recent administrative activity</h2><a href="{{ route('admin.audit.index') }}">View all</a></div><div class="table-responsive"><table class="table table-sm align-middle"><tbody>@forelse($recentAudit as $entry)<tr><td>{{ $entry->action }}</td><td class="text-muted">{{ $entry->actor?->email }}</td><td class="text-muted">{{ $entry->created_at?->diffForHumans() }}</td></tr>@empty<tr><td class="text-muted">No administrative activity recorded.</td></tr>@endforelse</tbody></table></div></div></div></div><div class="col-lg-5"><div class="card border-0 shadow-sm"><div class="card-body"><h2 class="h5">Payment status</h2><p class="text-muted mb-0">Payment data unavailable until the centralized Chapa phase.</p></div></div></div></div></div>
@endsection
