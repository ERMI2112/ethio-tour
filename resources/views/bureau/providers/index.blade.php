@extends('layouts.app')
@section('title', 'Provider Verification')
@section('content')
<div class="container-fluid py-4 py-lg-5"><div class="ws-page-header mb-4"><div><span class="ws-eyebrow"><span class="ws-eye-dot" aria-hidden="true"></span>Regulatory operations</span><h1 class="ws-title">Provider verification</h1><p class="ws-lead">Verify tourism businesses for Administrator activation review. This page never activates a platform account.</p></div></div><form class="card card-body border-0 shadow-sm mb-4" method="GET"><div class="row g-2 align-items-end"><div class="col-md-4"><label class="form-label" for="provider-search">Search</label><input id="provider-search" class="form-control" name="q" value="{{ $search }}" placeholder="Business name or owner email"></div><div class="col-md-3"><label class="form-label" for="provider-type">Provider type</label><select id="provider-type" class="form-select" name="type"><option value="">All types</option>@foreach(['hotel','restaurant','transportation_car_rental','event_organizer'] as $option)<option value="{{ $option }}" @selected($type === $option)>{{ str_replace('_',' ',ucfirst($option)) }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="provider-status">Verification status</label><select id="provider-status" class="form-select" name="status"><option value="">All statuses</option>@foreach(['pending','verified','rejected'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst($option) }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="provider-platform-status">Platform status</label><select id="provider-platform-status" class="form-select" name="platform_status"><option value="">All statuses</option>@foreach(['pending','approved','suspended','rejected'] as $option)<option value="{{ $option }}" @selected($platformStatus === $option)>{{ ucfirst($option) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Apply filters</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Business</th><th>Type</th><th>Owner</th><th>Bureau verification</th><th>Platform status</th><th>Registered</th><th><span class="visually-hidden">Action</span></th></tr></thead><tbody>@forelse($providers as $provider)<tr><td class="fw-semibold">{{ $provider->business_name }}</td><td>{{ str_replace('_',' ',ucfirst($provider->provider_type)) }}</td><td>{{ $provider->user?->email ?? 'Not provided' }}</td><td><x-ui.status-badge :status="$provider->verification_status" /></td><td><x-ui.status-badge :status="$provider->status" /></td><td class="text-nowrap">{{ $provider->created_at?->format('Y-m-d') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('bureau.providers.show', $provider) }}">Review</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No providers match these filters. Pending submissions will appear here when available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $providers->links() }}</div></div></div>
@endsection
