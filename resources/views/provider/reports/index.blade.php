@extends('layouts.app')
@section('title', 'Provider Reports')
@section('content')<div class="container py-4"><h1 class="h3 mb-4">{{ $provider->business_name }} analytics</h1><div class="row g-3 mb-4">@foreach([['Services',$serviceCount],['Bookings',$bookingTotal],['Reviews',$reviewCount],['Average rating',$reviewAverage === null ? '—' : number_format((float) $reviewAverage, 1)]] as [$label,$value])<div class="col-6 col-xl-3"><div class="card border-0 shadow-sm"><div class="card-body"><div class="small text-muted">{{ $label }}</div><div class="display-6 fw-semibold">{{ $value }}</div></div></div></div>@endforeach</div><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Booking status</h2></div><div class="card-body">@forelse($bookingStatuses as $status => $total)<div class="d-flex justify-content-between border-bottom py-2"><span>{{ ucfirst(str_replace('_',' ', $status)) }}</span><strong>{{ $total }}</strong></div>@empty<p class="text-muted mb-0">No bookings yet.</p>@endforelse</div></div></div>@endsection
