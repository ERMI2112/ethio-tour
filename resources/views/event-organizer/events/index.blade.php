@extends('layouts.app')
@section('title','My Events')
@section('content')<div class="container py-4"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Cultural programming</p><h1 class="h3 mb-1">My events</h1><p class="text-muted mb-0">Manage published events, schedules, and ticket capacity.</p></div><a class="btn btn-primary" href="{{ route('event-organizer.events.create') }}">Create event</a></div>@if($events->isEmpty())<x-ui.empty-state title="No events yet" message="Create your first cultural event." />@else<div class="table-responsive"><table class="table bg-white shadow-sm align-middle" aria-label="My events"><thead><tr><th scope="col">Event</th><th scope="col">Date</th><th scope="col">Venue</th><th scope="col">Status</th><th scope="col"><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@foreach($events as $event)<tr><td>{{ $event->event_name }}</td><td class="text-nowrap">{{ $event->event_date->format('M d, Y') }}</td><td>{{ $event->venue }}</td><td><x-ui.status-badge :status="$event->status" /></td><td><div class="d-flex flex-wrap gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('event-organizer.events.show',$event) }}">View</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('event-organizer.events.edit',$event) }}">Edit</a></div></td></tr>@endforeach</tbody></table></div>@endif</div>@endsection
