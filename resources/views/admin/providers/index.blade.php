@extends('layouts.app')
@section('title', 'Provider Activation')
@section('content')
<div class="container-fluid py-4"><h1 class="h3">Provider Governance</h1><p class="text-muted">Only Bureau-verified providers are eligible for platform activation.</p><form class="row g-2 mb-3"><div class="col-md-4"><input class="form-control" name="q" value="{{ $search }}" placeholder="Search business"></div><div class="col-md-2"><select class="form-select" name="type"><option value="">All types</option>@foreach(['hotel','restaurant','transportation_car_rental','event_organizer'] as $option)<option value="{{ $option }}" @selected($type === $option)>{{ str_replace('_',' ',ucfirst($option)) }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="verification"><option value="">All Bureau states</option>@foreach(['pending','verified','rejected'] as $option)<option value="{{ $option }}" @selected($verification === $option)>{{ ucfirst($option) }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="status"><option value="">All platform states</option>@foreach(['pending','approved','suspended','rejected'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst($option) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Business</th><th>Type</th><th>Bureau verification</th><th>Platform status</th><th>Account</th><th>Registered</th><th></th></tr></thead><tbody>@forelse($providers as $provider)<tr><td>{{ $provider->business_name }}</td><td>{{ str_replace('_',' ',ucfirst($provider->provider_type)) }}</td><td><x-ui.status-badge :status="$provider->verification_status" /></td><td><x-ui.status-badge :status="$provider->status" /></td><td>{{ $provider->user?->is_active ? 'Active' : 'Inactive' }}</td><td>{{ $provider->created_at?->format('Y-m-d') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.providers.show',$provider) }}">Review</a></td></tr>@empty<tr><td colspan="7" class="text-muted">No providers found.</td></tr>@endforelse</tbody></table></div>{{ $providers->links() }}</div>
@endsection
