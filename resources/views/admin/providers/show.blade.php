@extends('layouts.app')
@section('title', 'Provider Review')
@section('content')
<div class="container py-4 py-lg-5"><nav aria-label="Breadcrumb" class="mb-3"><a href="{{ route('admin.providers.index') }}">Provider governance</a> <span class="text-muted">/ Review</span></nav><div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Governance review</p><h1 class="h2 mb-1">{{ $provider->business_name }}</h1><p class="text-muted mb-0">{{ str_replace('_',' ',ucfirst($provider->provider_type)) }} · {{ $provider->user?->email }}</p></div><a class="btn btn-outline-secondary" href="{{ route('admin.providers.index') }}">Back to providers</a></div>
<div class="row g-4"><div class="col-lg-7"><div class="card border-0 shadow-sm mb-4"><div class="card-header bg-white"><h2 class="h5 mb-0">Provider and account</h2></div><div class="card-body"><dl class="row mb-0"><dt class="col-sm-4">Business</dt><dd class="col-sm-8">{{ $provider->business_name }}</dd><dt class="col-sm-4">Provider type</dt><dd class="col-sm-8">{{ str_replace('_',' ',ucfirst($provider->provider_type)) }}</dd><dt class="col-sm-4">Account</dt><dd class="col-sm-8">{{ $provider->user?->is_active ? 'Active' : 'Inactive' }}</dd><dt class="col-sm-4">Registered</dt><dd class="col-sm-8">{{ $provider->created_at?->format('Y-m-d H:i') }}</dd></dl></div></div><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Verification notes</h2></div><div class="card-body">{{ $provider->verification_notes ?: 'No verification notes have been recorded.' }}</div></div></div><div class="col-lg-5"><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Governance state</h2></div><div class="card-body"><div class="d-flex justify-content-between border-bottom pb-3 mb-3"><span>Bureau verification</span><x-ui.status-badge :status="$provider->verification_status" /></div><div class="d-flex justify-content-between mb-4"><span>Platform status</span><x-ui.status-badge :status="$provider->status" /></div>@if($provider->verification_status === 'verified')<p class="small text-muted">Administrator actions change only the platform status. Bureau verification is not editable here.</p><form method="POST" action="{{ route('admin.providers.status',$provider) }}" class="d-flex flex-wrap gap-2" onsubmit="return confirm('Apply this provider status change?');">@csrf @method('PATCH')@if($provider->status === 'pending')<button class="btn btn-success" name="status" value="approved">Approve for platform</button><button class="btn btn-outline-danger" name="status" value="rejected">Reject activation</button>@elseif($provider->status === 'approved')<button class="btn btn-outline-warning" name="status" value="suspended">Suspend operations</button>@elseif($provider->status === 'suspended')<button class="btn btn-success" name="status" value="approved">Reinstate operations</button>@endif</form>@else<p class="small text-muted mb-0">This provider is not eligible for Administrator activation until the Bureau marks it verified.</p>@endif</div></div></div></div>
</div>
@endsection
