@extends('layouts.app')
@section('title','Vehicle Inventory')
@section('content')<div class="container py-4"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Fleet operations</p><h1 class="h3 mb-1">Vehicle inventory</h1><p class="text-muted mb-0">Maintain active rental vehicles for your services.</p></div><a class="btn btn-primary" href="{{ route('transportation.vehicles.create') }}">Add vehicle</a></div>@if($vehicles->isEmpty())<x-ui.empty-state title="No vehicles yet" message="Add a physical vehicle to your transportation inventory." />@else<div class="table-responsive"><table class="table bg-white shadow-sm align-middle" aria-label="Vehicle inventory"><thead><tr><th scope="col">Identifier</th><th scope="col">Service</th><th scope="col">Type</th><th scope="col">Capacity</th><th scope="col">Status</th><th scope="col"><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@foreach($vehicles as $vehicle)<tr><td>{{ $vehicle->vehicle_identifier }}</td><td>{{ $vehicle->tourismService->service_name }}</td><td>{{ $vehicle->vehicle_type }}</td><td>{{ $vehicle->capacity }}</td><td><x-ui.status-badge :status="$vehicle->status" /></td><td><div class="d-flex flex-wrap gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('transportation.vehicles.edit',$vehicle) }}">Edit</a><form method="POST" action="{{ route('transportation.vehicles.destroy',$vehicle) }}" data-confirm="Remove this vehicle?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" type="submit">Remove</button></form></div></td></tr>@endforeach</tbody></table></div>@endif</div>@endsection
