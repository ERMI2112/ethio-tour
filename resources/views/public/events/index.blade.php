@extends('layouts.app')
@section('title','Cultural Events')
@section('content')<div class="container py-4"><div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><p class="text-uppercase small text-primary fw-semibold mb-1">Discover culture</p><h1 class="h2">Cultural Events</h1><p class="text-muted mb-0">Find published festivals and cultural events.</p></div><form class="d-flex gap-2" method="GET" action="{{ route('events.index') }}"><input name="q" value="{{ $search }}" class="form-control" placeholder="Search events..."><button class="btn btn-primary">Search</button></form></div>@if($events->isEmpty())<x-ui.empty-state title="No events found" message="Try another search or check back for new events." />@else<div class="row g-4">@foreach($events as $event)<div class="col-md-6 col-xl-4"><article class="card h-100 border-0 shadow-sm"><div class="card-body d-flex flex-column"><h2 class="h5">{{ $event->event_name }}</h2><p class="small text-muted">{{ $event->event_date->format('F d, Y') }} · {{ $event->venue }}</p><p>{{ $event->description }}</p><a class="btn btn-outline-primary mt-auto align-self-start" href="{{ route('events.show',$event) }}">View event</a></div></article></div>@endforeach</div>@endif</div>@endsection
