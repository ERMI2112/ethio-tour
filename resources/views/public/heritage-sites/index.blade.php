@extends('layouts.app')
@section('title', 'Heritage Sites')
@section('content')
    <div class="container py-5"><div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><h1 class="h2 mb-1">Heritage Sites</h1><p class="text-muted mb-0">Explore historical and cultural sites associated with tourism destinations.</p></div><form class="d-flex gap-2" method="GET" action="{{ route('heritage-sites.index') }}"><label class="visually-hidden" for="heritage-search">Search heritage sites</label><input class="form-control" id="heritage-search" name="q" placeholder="Search type or destination" value="{{ $search }}"><x-ui.button variant="outline-primary" type="submit">Search</x-ui.button></form></div>@if($heritageSites->isEmpty())<x-ui.empty-state title="No heritage sites found" message="Try another search term or return later for published heritage information." />@else<div class="row g-4">@foreach($heritageSites as $heritageSite)<div class="col-md-6 col-lg-4"><div class="card h-100 border-0 shadow-sm"><div class="card-body"><p class="text-uppercase text-muted small">{{ $heritageSite->destination->name }}</p><h2 class="h5"><a class="stretched-link text-decoration-none" href="{{ route('heritage-sites.show', $heritageSite) }}">{{ $heritageSite->heritage_type }}</a></h2><p class="small text-muted mb-0">Open: {{ $heritageSite->opening_hours }}</p></div></div></div>@endforeach</div>@endif</div>
@endsection
