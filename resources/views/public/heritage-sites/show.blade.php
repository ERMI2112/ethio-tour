@extends('layouts.app')
@section('title', $heritageSite->heritage_type)
@section('content')
<div class="container public-catalog-page py-4 py-lg-5"><a class="text-decoration-none small" href="{{ route('heritage-sites.index') }}">&larr; All heritage sites</a><article class="public-catalog-card mt-3" data-aos="fade-up"><div class="card-body p-4 p-md-5"><p class="text-uppercase text-muted small">Heritage site · <a href="{{ route('destinations.show', $heritageSite->destination) }}">{{ $heritageSite->destination->name }}</a></p><div class="d-flex flex-wrap justify-content-between align-items-start gap-2"><h1 class="h2 mb-0">{{ $heritageSite->heritage_type }}</h1>@if($heritageSite->latitude !== null && $heritageSite->longitude !== null)<a class="btn btn-outline-success btn-sm" href="{{ route('map', ['category' => 'heritage_sites', 'q' => $heritageSite->heritage_type]) }}">View on map</a>@endif</div><dl class="row mb-0"><dt class="col-sm-3">Opening hours</dt><dd class="col-sm-9">{{ $heritageSite->opening_hours }}</dd><dt class="col-sm-3">Entrance fee</dt><dd class="col-sm-9">{{ number_format($heritageSite->entrance_fee, 2) }}</dd></dl></div></article></div>
@endsection
