@extends('layouts.app')
@section('title', 'Audit Log')
@section('content')
<div class="container-fluid py-4 py-lg-5"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Administrator workspace</p><h1 class="h2 mb-1">Audit log</h1><p class="text-muted mb-0">Trace material platform changes with actor, action, target, and time.</p></div><form class="card card-body border-0 shadow-sm mb-4" method="GET"><div class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label" for="audit-search">Search actions</label><input id="audit-search" class="form-control" name="q" value="{{ $search }}" placeholder="approval, suspension, update"></div><div class="col-md-5"><label class="form-label" for="audit-action">Action</label><select id="audit-action" class="form-select" name="action"><option value="">All actions</option>@foreach($actions as $option)<option value="{{ $option }}" @selected($action === $option)>{{ str_replace('_',' ',ucfirst($option)) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Apply filters</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><caption class="visually-hidden">Administrative audit events</caption><thead class="table-light"><tr><th>Timestamp</th><th>Actor</th><th>Action</th><th>Target</th><th>Details</th></tr></thead><tbody>@forelse($entries as $entry)<tr><td class="text-nowrap">{{ $entry->created_at?->format('Y-m-d H:i') }}</td><td>{{ $entry->actor?->email ?? 'System' }}<div class="small text-muted">{{ $entry->actor?->role }}</div></td><td class="fw-semibold">{{ str_replace('_',' ',ucfirst($entry->action)) }}</td><td>{{ $entry->subject_type ? class_basename($entry->subject_type).' #'.$entry->subject_id : 'Platform' }}</td><td>@if($entry->metadata)<details><summary class="small text-primary">View recorded fields</summary><dl class="small mb-0 mt-2">@foreach($entry->metadata as $key => $value)<dt class="text-muted">{{ str_replace('_',' ',ucfirst($key)) }}</dt><dd>{{ is_scalar($value) ? $value : 'Recorded value' }}</dd>@endforeach</dl></details>@else<span class="text-muted">—</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No audit activity matches these filters.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $entries->links() }}</div></div></div>
@endsection
