@extends('layouts.app')
@section('title', 'Guide Verification Review')
@section('content')
<div class="container py-4 py-lg-5"><nav class="mb-3" aria-label="Breadcrumb"><a href="{{ route('bureau.guides.index') }}">Guide verification</a> <span class="text-muted">/ Review</span></nav><div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Regulatory review</p><h1 class="h2 mb-1">{{ $guide->user?->email ?? 'Guide' }}</h1><p class="text-muted mb-0">Review the information currently available for this guide.</p></div><a class="btn btn-outline-secondary" href="{{ route('bureau.guides.index') }}">Back to queue</a></div><div class="row g-4"><div class="col-lg-7"><div class="card border-0 shadow-sm mb-4"><div class="card-header bg-white"><h2 class="h5 mb-0">Guide identity</h2></div><div class="card-body"><dl class="row mb-0"><dt class="col-sm-4">Account</dt><dd class="col-sm-8">{{ $guide->user?->email ?? 'Not provided' }}</dd><dt class="col-sm-4">Registration date</dt><dd class="col-sm-8">{{ $guide->created_at?->format('Y-m-d') }}</dd></dl></div></div><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Professional information</h2></div><div class="card-body"><dl class="row mb-0"><dt class="col-sm-4">License</dt><dd class="col-sm-8">{{ $guide->license_number ?: 'Not provided' }}</dd><dt class="col-sm-4">Expertise</dt><dd class="col-sm-8">{{ $guide->expertise ?: 'Not provided' }}</dd><dt class="col-sm-4">Availability</dt><dd class="col-sm-8">{{ $guide->availability_status ? ucfirst($guide->availability_status) : 'Not provided' }}</dd></dl></div></div></div><div class="col-lg-5"><div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Verification state</h2></div><div class="card-body"><div class="d-flex justify-content-between border-bottom pb-3 mb-3"><span>Status</span><x-ui.status-badge :status="$guide->verification_status" /></div><p class="small text-muted">Existing notes: {{ $guide->verification_notes ?: 'Not provided' }}</p>@if($guide->verification_status === 'pending')<form method="POST" action="{{ route('bureau.guides.decide', $guide) }}" onsubmit="return confirm('Record this verification decision?');">@csrf @method('PATCH')<div class="mb-3"><label class="form-label" for="verification_notes">Decision notes</label><textarea class="form-control @error('verification_notes') is-invalid @enderror" id="verification_notes" name="verification_notes" rows="4">{{ old('verification_notes') }}</textarea>@error('verification_notes')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="d-flex flex-wrap gap-2"><button class="btn btn-success" name="decision" value="approve">Verify guide</button><button class="btn btn-outline-danger" name="decision" value="reject">Reject guide</button></div></form>@else<p class="small text-muted mb-0">This guide has already received a Bureau decision.</p>@endif</div></div></div></div></div>
@endsection
