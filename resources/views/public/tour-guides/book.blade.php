@extends('layouts.app')

@section('title', 'Request a Tour Guide')

@section('content')
<div class="container py-4 py-lg-5"><div class="row justify-content-center"><div class="col-lg-8"><nav aria-label="breadcrumb"><ol class="breadcrumb small"><li class="breadcrumb-item"><a href="{{ route('tour-guides.index') }}">Tour Guides</a></li><li class="breadcrumb-item"><a href="{{ route('tour-guides.show', $guide) }}">Profile</a></li><li class="breadcrumb-item active" aria-current="page">Request</li></ol></nav><div class="card border-0 shadow-sm"><div class="card-body p-4 p-md-5"><h1 class="h3 mb-1">Request Tour Guide</h1><p class="text-muted mb-4">Verified guide · {{ $guide->expertise }} · License {{ $guide->license_number }}</p>@include('layouts.partials.flash-messages')<form method="POST" action="{{ route('tour-guides.book.store', $guide) }}">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label" for="start_date">Tour start date</label><input class="form-control @error('start_date') is-invalid @enderror" type="date" id="start_date" name="start_date" value="{{ old('start_date') }}" min="{{ now()->toDateString() }}" required>@error('start_date')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label" for="end_date">Tour end date</label><input class="form-control @error('end_date') is-invalid @enderror" type="date" id="end_date" name="end_date" value="{{ old('end_date') }}" min="{{ now()->addDay()->toDateString() }}" required>@error('end_date')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label" for="number_of_tourists">Number of tourists</label><input class="form-control @error('number_of_tourists') is-invalid @enderror" type="number" id="number_of_tourists" name="number_of_tourists" value="{{ old('number_of_tourists', 1) }}" min="1" required>@error('number_of_tourists')<div class="invalid-feedback">{{ $message }}</div>@enderror</div></div><div class="alert alert-info small mt-4">Your request starts as <strong>Pending</strong>. The guide must review it before the central booking can be accepted. No payment is taken in this phase.</div><div class="d-flex gap-2"><button class="btn btn-primary" type="submit">Submit booking request</button><a class="btn btn-outline-secondary" href="{{ route('tour-guides.show', $guide) }}">Cancel</a></div></form></div></div></div></div></div>
@endsection
