@extends('layouts.app')
@section('title', 'Guide Verification')
@section('content')
<div class="container-fluid py-4 py-lg-5"><div class="ws-page-header mb-4"><div><span class="ws-eyebrow"><span class="ws-eye-dot" aria-hidden="true"></span>Regulatory operations</span><h1 class="ws-title">Guide verification</h1><p class="ws-lead">Review guide credentials and record a Bureau verification decision.</p></div></div><form class="card card-body border-0 shadow-sm mb-4" method="GET"><div class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label" for="guide-search">Search</label><input id="guide-search" class="form-control" name="q" value="{{ $search }}" placeholder="Email, license, or expertise"></div><div class="col-md-5"><label class="form-label" for="guide-status">Verification status</label><select id="guide-status" class="form-select" name="status"><option value="">All statuses</option>@foreach(['pending','verified','rejected'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst($option) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Apply filters</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Guide</th><th>License</th><th>Expertise</th><th>Status</th><th>Updated</th><th><span class="visually-hidden">Action</span></th></tr></thead><tbody>@forelse($guides as $guide)<tr><td class="fw-semibold">{{ $guide->user?->email ?? 'Not provided' }}</td><td>{{ $guide->license_number ?: 'Not provided' }}</td><td>{{ Str::limit($guide->expertise ?: 'Not provided', 70) }}</td><td><x-ui.status-badge :status="$guide->verification_status" /></td><td class="text-nowrap">{{ $guide->updated_at?->format('Y-m-d') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('bureau.guides.show', $guide) }}">Review</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No guides match these filters. Pending submissions will appear here when available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $guides->links() }}</div></div></div>
@endsection
