@extends('layouts.app')
@section('title', 'User Management')
@section('content')
<div class="container-fluid py-4 py-lg-5"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Administrator workspace</p><h1 class="h2 mb-1">Users</h1><p class="text-muted mb-0">Review account access without exposing credentials or changing roles from this workspace.</p></div><form class="card card-body border-0 shadow-sm mb-4" method="GET"><div class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label" for="user-search">Search by email</label><input id="user-search" class="form-control" name="q" value="{{ $search }}" placeholder="name@example.com"></div><div class="col-md-3"><label class="form-label" for="user-role">Role</label><select id="user-role" class="form-select" name="role"><option value="">All roles</option>@foreach(['tourist','tour_guide','service_provider','tourism_bureau_officer','administrator'] as $option)<option value="{{ $option }}" @selected($role === $option)>{{ str_replace('_',' ',ucfirst($option)) }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label" for="user-status">Account state</label><select id="user-status" class="form-select" name="active"><option value="">All accounts</option><option value="1" @selected($active === '1')>Active</option><option value="0" @selected($active === '0')>Inactive</option></select></div><div class="col-md-2"><button class="btn btn-primary w-100">Apply filters</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><caption class="visually-hidden">Platform users</caption><thead class="table-light"><tr><th>Email</th><th>Role</th><th>Account state</th><th>Created</th><th>Updated</th><th><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@forelse($users as $user)<tr><td class="fw-semibold">{{ $user->email }}</td><td>{{ str_replace('_',' ',ucfirst($user->role)) }}</td><td><x-ui.status-badge :status="$user->is_active ? 'active' : 'inactive'" /></td><td class="text-nowrap">{{ $user->created_at?->format('Y-m-d') }}</td><td class="text-nowrap">{{ $user->updated_at?->format('Y-m-d') }}</td><td>@if($user->user_id !== auth()->id())<form method="POST" action="{{ route('admin.users.toggle',$user) }}" onsubmit="return confirm('{{ $user->is_active ? 'Deactivate this account?' : 'Activate this account?' }}');">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">{{ $user->is_active ? 'Deactivate' : 'Activate' }}</button></form>@else<span class="text-muted small">Current account protected</span>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No users match these filters.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $users->links() }}</div></div></div>
@endsection
