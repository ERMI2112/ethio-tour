@extends('layouts.app')
@section('title', 'Audit Log')
@section('content')
<div class="container-fluid py-4"><h1 class="h3">Audit Log</h1><form class="row g-2 mb-3"><div class="col-md-5"><input class="form-control" name="q" value="{{ $search }}" placeholder="Search action"></div><div class="col-md-5"><select class="form-select" name="action"><option value="">All actions</option>@foreach($actions as $option)<option value="{{ $option }}" @selected($action === $option)>{{ str_replace('_',' ',ucfirst($option)) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Timestamp</th><th>Actor</th><th>Action</th><th>Target</th><th>Details</th></tr></thead><tbody>@forelse($entries as $entry)<tr><td>{{ $entry->created_at }}</td><td>{{ $entry->actor?->email }}<div class="small text-muted">{{ $entry->actor?->role }}</div></td><td>{{ str_replace('_',' ',ucfirst($entry->action)) }}</td><td>{{ class_basename($entry->subject_type ?? 'Platform') }} #{{ $entry->subject_id ?? '—' }}</td><td>{{ $entry->metadata ? json_encode($entry->metadata) : '—' }}</td></tr>@empty<tr><td colspan="5">No audit activity recorded.</td></tr>@endforelse</tbody></table></div>{{ $entries->links() }}</div>
@endsection
