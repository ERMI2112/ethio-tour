@extends('layouts.app')

@section('title', 'Tour Guides')

@section('content')
<div class="container py-4 py-lg-5"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><p class="text-muted small text-uppercase mb-1">Public directory</p><h1 class="h2 mb-1">Find a Tour Guide</h1><p class="text-muted mb-0">Browse guides whose profiles have been verified by the platform.</p></div></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body row g-3 align-items-end"><div class="col-md-7"><label class="form-label" for="q">Search expertise</label><input class="form-control" id="q" name="q" value="{{ $search }}" placeholder="e.g. history, heritage, city tours"></div><div class="col-md-3"><label class="form-label" for="availability">Availability</label><select class="form-select" id="availability" name="availability"><option value="">Any status</option><option value="available" @selected($availability === 'available')>Available</option><option value="unavailable" @selected($availability === 'unavailable')>Unavailable</option></select></div><div class="col-md-2"><button class="btn btn-primary w-100" type="submit">Search</button></div></div></form><div class="row g-4">@forelse ($guides as $guide)<div class="col-md-6 col-xl-4"><article class="card border-0 shadow-sm h-100"><div class="card-body d-flex flex-column p-4"><div class="d-flex justify-content-between gap-2 mb-3"><span class="badge text-bg-success">Verified guide</span><span class="badge text-bg-{{ $guide->availability_status === 'available' ? 'success' : 'secondary' }}">{{ ucfirst($guide->availability_status) }}</span></div><h2 class="h5">Tour Guide Profile</h2><p class="text-muted small mb-2">License {{ $guide->license_number }}</p><p class="mb-3">{{ $guide->expertise }}</p><p class="small text-muted mb-4">Rating: {{ $guide->average_rating === null ? 'No ratings yet' : number_format((float) $guide->average_rating, 1).' / 5' }}</p><a class="btn btn-outline-primary mt-auto" href="{{ route('tour-guides.show', $guide) }}">View profile</a></div></article></div>@empty<div class="col-12"><div class="card border-0 shadow-sm"><div class="card-body p-5 text-center text-muted"><h2 class="h5">No verified guides found</h2><p class="mb-0">Try a different expertise search or availability filter.</p></div></div></div>@endforelse</div></div>
@endsection
