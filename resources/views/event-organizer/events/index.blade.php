@extends('layouts.app')
@section('title','My Events')
@section('content')<div class="container py-4"><div class="d-flex justify-content-between mb-4"><h1 class="h3">My events</h1><a class="btn btn-primary" href="{{ route('event-organizer.events.create') }}">Create event</a></div>@if($events->isEmpty())<x-ui.empty-state title="No events yet" message="Create your first cultural event." />@else<div class="table-responsive"><table class="table bg-white shadow-sm align-middle"><thead><tr><th>Event</th><th>Date</th><th>Venue</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach($events as $event)<tr><td>{{ $event->event_name }}</td><td>{{ $event->event_date->format('M d, Y') }}</td><td>{{ $event->venue }}</td><td>{{ ucfirst($event->status) }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('event-organizer.events.show',$event) }}">View</a> <a class="btn btn-sm btn-outline-secondary" href="{{ route('event-organizer.events.edit',$event) }}">Edit</a></td></tr>@endforeach</tbody></table></div>@endif</div>@endsection
